import React from "react";
import { GiphyList, Header, ProfileList, Input } from "./components";

export default () => {
  return (
    <div>
      {/* <Header />
      <GiphyList /> */}
      <Input />
      <ProfileList />
    </div>
  );
}